# Training Datasets

This directory will contain datasets used for training ML models:

- **Food-101**: Food image classification dataset (used for SegFormer training)
- **Labeled Food Images**: Curated images with portions and type annotations
- **Text Corpus**: Donation descriptions and organizational needs for NLP training

Large datasets should be managed separately from git (use .gitignore).
